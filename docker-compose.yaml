services:

  # template: 
  #   build:
  #     context: PATH_TO_DIR
  #   container_name: ${DJ_CONTAINER_NAME}
  #   env_file:
  #     - .env
  #   depends_on:
  #     - db
  #   networks:
  #     - rascendance-network
  #   ports:
  #     - ${BACKEND_PORT}:${BACKEND_PORT}
  #   expose:
  #     - ${POSTGRES_PORT}
  #   volumes:
  #     - ./PATH_TO_DIR/code:/code
  #   command: python /code/manage.py runserver 0.0.0.0:${BACKEND_PORT}
  # nginx:
  #  image: nginx:latest 
  #  container_name: ${NGINX_CONTAINER_NAME}
  #  env_file:
  #    - .env
  #  depends_on:
  #    - db

  nginx:
    build:
      context: nginx-files 
    container_name: ${NGINX_CONTAINER_NAME}
    env_file:
      - .env
    depends_on:
      - db
    volumes:
      - ./nginx-files/static-files:/etc/nginx/html
    ports:
      - 7000:443
    networks:
      - websocket-network
      - rascendance-network

  db:
    restart: always
    image: postgres:latest
    container_name: ${DB_CONTAINER_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    expose:
      - ${POSTGRES_PORT}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    networks:
      - rascendance-network

  usermng:
    restart: always
    build:
      context: ./UserMng
      dockerfile: Dockerfile
    container_name: UserMng
    expose:
      - ${BACKEND_PORT}
    volumes:
      - ./UserMng/code:/UserMng/code
    depends_on:
      - db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_PORT: ${POSTGRES_PORT}
      DJANGO_ALLOWED_HOSTS: ${DJANGO_ALLOWED_HOSTS}
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY}
      DEBUG: ${DEBUG}
      DATABASE_ENGINE: ${DATABASE_ENGINE}
      DATABASE_HOST: ${DB_HOST}
      BACKEND_PORT: ${BACKEND_PORT}
      DJANGO_SUPERUSER_PASSWORD: ${DJANGO_SUPERUSER_PASSWORD}
      SUPER_USER: ${SUPER_USER}
      SUPER_EMAIL: ${SUPER_EMAIL}
    networks:
      - rascendance-network

# websocket

  websocket: 
    build:
      context: websocket
    container_name: ${WEBSOCKET_NAME}
    env_file:
      - .env
    depends_on:
      - web-db
    networks:
      - websocket-network
    expose:
      - ${WEBSOCKET_PORT}
    volumes:
      - ./websocket/code:/code
    command: python /code/manage.py runserver 0.0.0.0:${WEBSOCKET_PORT} && python /code/manage.py runworker game_engine

  redis:
    image: redis
    command: --port ${WS_REDIS_PORT}
    container_name: redis
    expose:
      -  ${WS_REDIS_PORT}
    networks:
      - websocket-network

  web-db:
    restart: always
    image: postgres:latest
    container_name: ${WS_DATABASE_NAME}
    volumes:
      - websocket_data:/var/lib/postgresql/data
    expose:
      - ${WS_DATABASE_PORT}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    networks:
      - websocket-network

volumes:
  postgres_data:
    name: postgres_data
  websocket_data:
    name: websocket_data

networks:
  rascendance-network:
    driver: bridge
  websocket-network:
    driver: bridge
